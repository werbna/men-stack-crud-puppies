<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Puppy Details</title>
</head>
<body>
  <div>
    <h1>Puppy Details</h1>
    <div>
      <p><strong>Name:</strong> <%= puppy.name %></p>
      <p><strong>Breed:</strong> <%= puppy.breed %></p>
      <p><strong>Coat:</strong> <%= puppy.coat || 'Not specified' %></p>
      <p><strong>Age:</strong> <%= puppy.age %> years</p>
      <p><strong>Good with Children:</strong> <%= puppy.isGoodWithChildren ? "Yes" : "No" %></p>
    </div>
    <div>
      <form action="/puppies/<%= puppy._id %>?_method=DELETE" method="POST" style="display: inline;">
        <button type="submit">Delete</button>
      </form>
      <a href="/puppies/<%= puppy._id %>/edit">
        <button type="submit">Edit</button>
      </a>
    </div>
    <a href="/puppies">Back to Puppies List</a>
  </div>
  </div>
</body>
</html>
